# Происхождение кода

Исходный код изначально разрабатывался по техническому заданию заказчика и публиковался в репозитории:

**[Yakvenalex/EasyBookerBot](https://github.com/Yakvenalex/EasyBookerBot)**

Правообладатель сервиса EasyBooker принял решение распространять проект открыто под лицензией **MIT** (см. `LICENSE` в корне).

В этой копии:

- удалён секретный файл `.env` из истории; используйте `.env.example`;
- добавлены README, документация и явные поля `REDIS_*` в `app/config.py` для совместимости с модулем `redis_dao`;
- исправлено дублирование `include_router` в `app/main.py`.
